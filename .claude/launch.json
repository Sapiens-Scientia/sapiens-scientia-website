{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "[ -e node_modules ] || ln -s \"$(git rev-parse --git-common-dir)/../node_modules\" node_modules; npm run dev"],
      "port": 3000,
      "autoPort": true
    }
  ]
}
